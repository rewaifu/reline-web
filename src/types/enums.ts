export enum NodeActionType {
  ADD = "ADD",
  DELETE = "DELETE",
  CHANGE = "CHANGE",
  MOVEUP = "MOVEUP",
  MOVEDOWN = "MOVEDOWN",
}

export enum ReaderNodeMode {
  RGB = "rgb",
  GRAY = "gray",
  DYNAMIC = "dynamic",
}

export enum NodeType {
  LEVEL = "level",
  FOLDER_READER = "folder_reader",
}
